# Configuration

El archivo `config.yml` se genera automáticamente.

```yaml
config-version: 1

ignore:
  leather-armor: true
  turtle-helmet: false
  elytra: false

invisibility-potion:
  always-hide-gear: false

locale:
  default-locale: en_us
```
